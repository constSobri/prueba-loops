// Este codigo lo que hace es que toma la variable numeroFor para darle el valor a otra variable "i" (se que no es necesario pero asi lo quise y esta bueno para temas de practica), luego si "i" es menor o igual 3 entonces tendra que sumarse por 1 hasta que termine. Todo mientras se imprime en pantalla.

let numeroFor = 0;

for (i = numeroFor; 3 >= i; i++) {
  console.log(i);
}
