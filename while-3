// Este codigo verifica si la variable es menor que 3 y en caso de serlo lo incrementa hasta llegar a 3, ademas lo imprime en pantalla.

let numeroWhile = 0;

while (numeroWhile < 3) {
  numeroWhile++;
  console.log(numeroWhile);
}
